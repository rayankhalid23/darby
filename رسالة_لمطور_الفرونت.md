# 📩 تعديلات مطلوبة في لوحة تحكم المشرفين

السلام عليكم،

تم إصلاح مشكلة رفع الصورة من طرف السيرفر، وأُضيفت آلية جديدة لتغيير البريد الإلكتروني.
هذه كل التفاصيل من الصفر — لا تحتاج أي معرفة سابقة بأي شاشة أخرى في المشروع.

---

## القسم الأول: مشكلة الصورة — ✅ انتهت، ولا تعديل مطلوب منك

**السبب لم يكن من الفرونت.** كان في إعدادات PHP على السيرفر: لم يكن يجد مجلداً مؤقتاً
لحفظ الملف المرفوع، فيطبع تحذيراً بصيغة HTML **قبل** الـ JSON، فيفشل Dio في تحليل الرد
ويعطي `Status 0`.

تم إصلاحه واختباره: الرفع يرجع الآن **201** ومعه رابط الصورة كامل.

**كودك الحالي صحيح ولا يحتاج أي تغيير:**
- `POST /api/admin/admins` بـ `multipart/form-data`
- اسم حقل الصورة `avatar` ✔️
- الحد الأقصى **2 ميجابايت**، والصيغ المقبولة: `jpg` / `jpeg` / `png`

الشيء الوحيد المفيد: لو حجم الصورة أكبر من 2 ميجا سترجع **422** برسالة عربية واضحة
في `errors.avatar[0]` — اعرضها للمستخدم كما هي. ويفضّل ضغط الصورة قبل الرفع.

---

## القسم الثاني: تغيير البريد الإلكتروني — 🆕 يحتاج تعديلات منك

### الفكرة باختصار

عند تعديل بيانات مشرف وتغيير بريده الإلكتروني، **البريد لا يتغير فوراً**.

بدلاً من ذلك:
1. يبقى البريد القديم شغّالاً كما هو في قاعدة البيانات.
2. يرسل السيرفر **رابط تأكيد إلى البريد الجديد** صالح لمدة **30 دقيقة**.
3. البريد يتغير فعلياً **فقط** بعد أن يفتح صاحب البريد ذلك الرابط.

السبب: التأكد أن البريد الجديد يملكه فعلاً شخص حقيقي ويستطيع استقبال رسائل عليه.

### ماذا يعني هذا لك عملياً؟

بعد استدعاء تعديل المشرف، **لا تفترض أن البريد تغيّر**، وافتح للمستخدم نافذة انتظار
يتابع منها الحالة. وهذا كل التفصيل:

---

### 1) عند تعديل المشرف

```
POST /api/admin/admins/{id}
Authorization: Bearer {token}
Accept: application/json
```

الرد الآن فيه **مفتاح جديد اسمه `email_verification`** بجانب `data` (وليس داخله).

**حالة (أ) — البريد لم يتغيّر:** يكون `null` وتكمل عملك كالمعتاد.

```json
{
  "status": true,
  "message": "تم تحديث بيانات المشرف بنجاح.",
  "data": { "id": 6, "email": "old@derbi.ly", "email_change_pending": false, "pending_new_email": null },
  "email_verification": null
}
```

**حالة (ب) — البريد تغيّر:** يكون كائناً، وهنا **افتح نافذة الانتظار**.

```json
{
  "status": true,
  "message": "تم تحديث البيانات بنجاح. أرسلنا رابط تأكيد للبريد الجديد، يرجى فتحه لتفعيل التغيير.",
  "data": {
    "id": 6,
    "email": "old@derbi.ly",
    "email_change_pending": true,
    "pending_new_email": "new@derbi.ly"
  },
  "email_verification": {
    "status": "pending",
    "new_email": "new@derbi.ly",
    "expires_at": "2026-08-10 04:40:45"
  }
}
```

> ⚠️ لاحظ أن `data.email` **ما زال البريد القديم** — هذا صحيح ومقصود.
> البريد الجديد المنتظر تأكيده تجده في `data.pending_new_email`.

**الكود:**
```dart
final emailVerification = response.data['email_verification'];

if (emailVerification != null) {
  showEmailVerificationDialog(
    adminId: adminId,
    newEmail: emailVerification['new_email'],
  );
} else {
  showSuccess(response.data['message']);
  refreshList();
}
```

---

### 2) نافذة الانتظار المطلوبة

نافذة **غير قابلة للإغلاق بالضغط خارجها**، نصها مثلاً:

> أرسلنا رابط تأكيد إلى **new@derbi.ly**
> افتح بريدك واضغط على الرابط لتفعيل التغيير.
> الرابط صالح لمدة 30 دقيقة.

وفيها **ثلاثة أزرار**:

| الزر | يستدعي |
|---|---|
| ✅ تم التأكيد | نقطة فحص الحالة (رقم 3) |
| 🔁 إعادة إرسال الرابط | نقطة إعادة الإرسال (رقم 5) |
| ❌ إلغاء التعديل | نقطة الإلغاء (رقم 4) |

---

### 3) فحص الحالة — زر "تم التأكيد"

```
GET /api/admin/admins/{id}/email-change/status
Authorization: Bearer {token}
```

```json
{
  "status": true,
  "data": {
    "status": "pending",
    "new_email": "new@derbi.ly"
  }
}
```

القيم الأربع الممكنة لـ `data.status` وما تفعله في كل واحدة:

| القيمة | المعنى | ماذا تفعل |
|---|---|---|
| `pending` | لم يُفتح الرابط بعد | أبقِ النافذة مفتوحة + نبّه: "لم يتم التأكيد بعد، افتح الرابط في بريدك" |
| `verified` | ✅ تم التأكيد والبريد تغيّر فعلاً | أغلق النافذة + أعد تحميل البيانات + رسالة نجاح |
| `rejected` | المستخدم ضغط رابط الرفض | أغلق النافذة + نبّه: "تم رفض طلب تغيير البريد" |
| `expired` | انتهت الـ 30 دقيقة أو لا يوجد طلب | أغلق النافذة + نبّه: "انتهت صلاحية الرابط، أعد المحاولة" |

> عند `verified` و `rejected` و `expired` تكون `new_email` = `null`.

**الكود:**
```dart
final status = (await api.get('/api/admin/admins/$id/email-change/status'))
    .data['data']['status'];

switch (status) {
  case 'verified':
    closeDialog(); refreshList(); showSuccess('تم تأكيد البريد الإلكتروني بنجاح');
    break;
  case 'pending':
    showWarning('لم يتم التأكيد بعد، يرجى فتح الرابط في بريدك');
    break;
  case 'rejected':
    closeDialog(); showWarning('تم رفض طلب تغيير البريد');
    break;
  case 'expired':
  default:
    closeDialog(); showWarning('انتهت صلاحية الرابط، أعد المحاولة');
}
```

---

### 4) إلغاء الطلب — زر "إلغاء التعديل"

```
POST /api/admin/admins/{id}/email-change/cancel
Authorization: Bearer {token}
```

بدون Body.

**نجاح (200):**
```json
{ "status": true, "message": "تم إلغاء طلب تغيير البريد الإلكتروني." }
```

**لا يوجد طلب معلّق (400):**
```json
{ "status": false, "message": "لا يوجد طلب معلق لتغيير البريد الإلكتروني." }
```

بعد الإلغاء يتوقف الرابط القديم عن العمل ويبقى البريد القديم كما هو.

---

### 5) إعادة إرسال الرابط — زر "إعادة الإرسال"

```
POST /api/admin/admins/{id}/email-change/resend
Authorization: Bearer {token}
```

بدون Body.

```json
{
  "status": true,
  "message": "تمت إعادة إرسال رابط التأكيد بنجاح.",
  "email_verification": {
    "status": "pending",
    "new_email": "new@derbi.ly",
    "expires_at": "2026-08-10 05:10:45"
  }
}
```

> يُبطل الرابط القديم ويصدر رابطاً جديداً بمهلة 30 دقيقة جديدة.
> لو لا يوجد طلب معلّق ترجع **400**.

---

### 6) حقلان جديدان في كائن المشرف

أُضيفا إلى **كل** الردود (القائمة / العرض / الإضافة / التعديل):

| الحقل | النوع | الاستخدام |
|---|---|---|
| `email_change_pending` | boolean | `true` = هناك طلب تغيير بريد معلّق |
| `pending_new_email` | string \| null | البريد الجديد المنتظر تأكيده |

**استخدمهما في جدول المشرفين** لعرض شارة صفراء بجانب البريد:

```dart
if (admin.emailChangePending)
  Badge(
    color: Colors.amber,
    label: 'بانتظار تأكيد: ${admin.pendingNewEmail}',
  )
```

فائدتها: لو أغلق المستخدم التطبيق وفتحه لاحقاً، تعرف من القائمة مباشرة أن هناك
طلباً معلّقاً بدون أي استدعاء إضافي.

---

## ✅ ملخص التعديلات المطلوبة منك

1. أضف `emailChangePending` و `pendingNewEmail` إلى موديل المشرف (`AdminModel`).
2. اقرأ `email_verification` من رد التعديل — إن لم يكن `null` افتح نافذة الانتظار.
3. أنشئ نافذة الانتظار بأزرارها الثلاثة.
4. أضف الدوال الثلاث في الـ Remote Data Source:
   - `checkEmailChangeStatus(id)` → `GET  /api/admin/admins/{id}/email-change/status`
   - `cancelEmailChange(id)`      → `POST /api/admin/admins/{id}/email-change/cancel`
   - `resendEmailChange(id)`      → `POST /api/admin/admins/{id}/email-change/resend`
5. أضف شارة "بانتظار التأكيد" في جدول المشرفين.
6. لا تعرض `pending_new_email` كأنه البريد الحالي — البريد الحالي دائماً `data.email`.

## 🔒 لا شيء تغيّر في

إضافة مشرف، حذف مشرف، عرض القائمة، عرض مشرف واحد، ورفع الصورة — كلها بنفس
الشكل السابق تماماً ولا تحتاج تعديل.
